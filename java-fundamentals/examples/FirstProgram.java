// The class name must match the file name exactly
public class FirstProgram {

    // This is the main entry point. Without this method, the program cannot run!
    public static void main(String[] args) {
        // Customize the text inside quotes to change the console output
        System.out.println("La bebesita bebelin!");
    }
}